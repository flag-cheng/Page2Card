# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# page2card data (never commit the user's saved articles or generated cards)
data/*.db
data/cards/

# Environment / secrets
.env

# Local scratch helpers (single-underscore prefix, e.g. _scratch.py).
# Use a pattern that does NOT swallow dunder files like __init__.py.
_[!_]*.py

# OS / editor
.DS_Store
Thumbs.db

# Local Claude Code tooling (preview launch config, etc.)
.claude/
